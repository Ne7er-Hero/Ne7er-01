void* maq_timthread(void* arg) {
    printf("Thread started, arg = %p\n", arg);

    int counter = 0;
    while(1) {
        printf("Thread running, count = %d\n", counter++);
        sleep(1);
    }

    return NULL;
}
